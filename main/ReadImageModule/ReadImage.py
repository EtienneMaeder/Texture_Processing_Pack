# TODO: Create file that reads image data (png/tga)
# Class for image
# Load image and swap channels
# read / write image
# copy image


class image:

    def __init__(self, filepath):
        self.sourcePath = filepath
        return

    # Changes channel from source to destination
    def changeChannel(self, sourceChannel, destinationChannel):

        return

    # export image
    def exportImage(self, destinationPath):

        return
